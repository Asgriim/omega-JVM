project(omega_jvm)

#file(GLOB sources ./*.cpp)

#include_directories(${INCLUDE_DIR})

#foreach(X IN LISTS sources)
#    string(REGEX MATCH "[^/]*\\.cpp"  OUT ${X})
#    message(${OUT})
#    target_link_libraries(${OUT} classfile)
#endforeach()



